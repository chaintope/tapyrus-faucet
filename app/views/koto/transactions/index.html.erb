<h1><a href="https://askmona.org/7853">Koto</a> Faucet</h1>

<div class="row">
  <aside class="col-md-2">
    <section class="send_to_address_form">
      <%= form_for([:koto, @transaction]) do |f| %>
        <%= render 'shared/error_messages' %>
        <%= f.label :address %>
        <%= f.text_field :address, class: 'form-control' %>
        <%= f.submit "Send", class: "btn btn-primary" %>
      <% end %>
      <p>Kotoを少量ではありますがさしあげます。</p>
      <h4>このサイトの保有量</h4>
      <span class="text-info"><%= Transaction.balance %> koto</span>
    </section>
  </aside>
  <div class="col-md-10">
    <% if @transactions.present? %>
    <table class="table table-bordered table-striped">
      <tr>
        <th>address</th>
        <th>value</th>
        <th>datetime</th>
      </tr>
      <% @transactions.each do |t| %>
      <tr>
        <td><%= t.address %></td>
        <td align="right"><%= t.value %></td>
        <td><%= t.created_at %></td>
      </tr>
      <% end %>
    </table>
    <%= will_paginate @transactions %>
    <% else %>
      <p>Not Found</p>
    <% end %>
    <div style="float:bottom;">
      <a href="https://zaif.jp?ac=gnitbf134g" rel="nofollow"><img src="https://d2p8taqyjofgrq.cloudfront.net/images/affiliate/banner/zaif_A_728x90.png?ac=gnitbf134g" /></a>
    </div>
  </div>
</div>
